"""Deprecated settings module kept only to avoid breaking old imports.

Use ``ecom.settings``. Production credentials must be supplied through environment
variables / the server-side .env file and must never be committed here.
"""
